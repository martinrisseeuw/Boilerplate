

/* Get all div elements. */
var fadeInRight = document.querySelectorAll(".bounceright");

/* Animate all divs at once. */
Velocity(fadeInRight, {translateX: 400, opacity: 1}, { delay: 2000 }); // Velocity
